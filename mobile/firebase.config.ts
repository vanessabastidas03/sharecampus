import firebase from '@react-native-firebase/app';

// @react-native-firebase lee automáticamente el GoogleService-Info.plist en iOS
// y google-services.json en Android, no necesitas pasar credenciales manualmente.

export default firebase;